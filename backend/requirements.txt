fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Optional GCP integrations — only required when DATA_BACKEND=bigquery /
# PUBSUB_ENABLED=true / AI_PROVIDER=vertex. Install as needed for deployment.
google-cloud-bigquery==3.25.0
google-genai>=2.9,<3
# google-cloud-pubsub==2.23.1
# Optional Razorpay test/sandbox SDK
# razorpay==1.4.2
